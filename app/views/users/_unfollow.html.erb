<%= form_for(current_user.active_relationships.find_by(followed_id: @user.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Unfollow", class: "btn btn-danger" %>
  <%= "following each other" if @user.mutual?(current_user) %>
<% end %>
